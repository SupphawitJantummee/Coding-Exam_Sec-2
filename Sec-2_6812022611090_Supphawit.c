#include <stdio.h>

int main() {
    int time ;
    float base_fee_toal_fee;
    char service;
    int O = [15];
    int B = [40];
    int A = [20];

    printf("ประเภทช่องทางธุรกรรม:\n")
    printf("ประเภท O ออนไลน์ ช 15 \n")
    printf("ประเภท B ที่สาขา = 40 บาท \n")
    printf("ประเภท A ตู้ ATM 20 บาท \n")
}
    printf("กรอกประเภทอัตราส่วนของค่าธรรมเนียม O,B,A")
    scanf("%c", &service);

    printf("กรอกจำนวนระหว่าง 3-12"):");
    scanf("%d", &time);

    if(times< 3, times >12){
    printf("จำนวนครั้งที่ต้องอยู่ระหว่าง 3-12 \n")
    return 0;
}
